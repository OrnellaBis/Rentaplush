<div class='home'>
  <h3 class="p-2 px-5">Nos peluches les plus populaires</h3>
    <div class="home-cards p-3 px-5">


      <% @plushes.each do |plush|   %>
        <%= link_to plush_path(plush) do %>
        <div class="home-card" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_tag plush.photo.key %>')">  <%= plush.name  %></div>
        <% end %>
      <% end %>
    </div>

      <h3 class="p-2 px-5">Nos catégories les plus populaires</h3>

        <div class="home-cards p-3 px-5">

          <div class="home-card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_path("Kawaii.jpg")%>')">
            <h4>Kawaii</h4>
          </div>

          <div class="home-card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_path("marvel.jpg")%>')">
            <h4>Films</h4>
          </div>

          <div class="home-card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_path("Panda.jpg")%>')">
            <h4>Animaux</h4>
          </div>

          <div class="home-card"  style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_path("emoji.jpg")%>')">

            <h4>Emoji</h4>
          </div>
        </div>
</div>
